# Production is served by Vercel (see vercel.json). This file exists only so the
# Netlify integration still attached to the repository builds instead of failing;
# delete it once that integration is disconnected.

[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# No SPA catch-all redirect: Astro emits dist/404.html and real directory
# index files for /de/, /impressum/ and /datenschutz/, which Netlify serves
# natively. A "/* -> /index.html" rule would return the English homepage
# for every one of those routes.

[[headers]]
  for = "/_astro/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

[[headers]]
  for = "/*"
  [headers.values]
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
    X-Frame-Options = "SAMEORIGIN"
    Permissions-Policy = "camera=(), microphone=(), geolocation=(), interest-cohort=()"
